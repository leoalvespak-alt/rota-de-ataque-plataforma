export const typography = Object.freeze({
  textXs: '12px',
  textSm: '14px',
  textBase: '16px',
  textLg: '18px',
  textXl: '20px',
  text2xl: '24px'
})

export const spacing = Object.freeze({
  space1: '4px',
  space2: '8px',
  space3: '12px',
  space4: '16px',
  space5: '20px',
  space6: '24px',
  space8: '32px',
  space10: '40px',
  space12: '48px'
})

export const radii = {
  sm: 'var(--radius-sm)',
  md: 'var(--radius-md)',
  lg: 'var(--radius-lg)',
  full: 'var(--radius-full)'
}

export const breakpoints = {
  mobile: '(max-width: 768px)',
  tablet: '(max-width: 1024px)',
  desktop: '(min-width: 1025px)'
}

export const radius = Object.freeze({
  radiusSm: '4px',
  radiusMd: '6px',
  radiusLg: '8px',
  radiusXl: '12px',
  radiusFull: '9999px'
})

export const elevation = Object.freeze({
  shadowSm: '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
  shadowMd: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
  shadowLg: '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)'
})

export const density = Object.freeze({
  comfortable: '12px',
  compact: '8px'
})

export const semanticTokens = Object.freeze({ surfaceCanvas:'var(--surface-canvas)',surfaceCard:'var(--surface-card)',surfaceSubtle:'var(--surface-subtle)',borderDefault:'var(--border-default)',borderStrong:'var(--border-strong)',textPrimary:'var(--text-primary)',textSecondary:'var(--text-secondary)',textTertiary:'var(--text-tertiary)',accentPrimary:'var(--accent-primary)',accentSecondary:'var(--accent-secondary)',statusSuccess:'var(--status-success)',statusWarn:'var(--status-warn)',statusError:'var(--status-error)',statusInfo:'var(--status-info)',scoreHigh:'var(--score-high)',scoreMed:'var(--score-med)',scoreLow:'var(--score-low)',roleCollector:'var(--role-collector)',roleActor:'var(--role-actor)'})
