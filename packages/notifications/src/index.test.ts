import{describe,expect,it}from'vitest';import{emailTemplate,fingerprint}from'./index.js';describe('notifications',()=>{it('deduplicates normalized errors',()=>expect(fingerprint('x','y','error 12')).toBe(fingerprint('x','y','error 99')));it('escapes HTML',()=>expect(emailTemplate({kind:'x',severity:'error',campaign:'c',message:'<script>',dashboardUrl:'https://x',traceId:'t'})).not.toContain('<script>'))})
