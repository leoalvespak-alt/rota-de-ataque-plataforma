import{createHmac,timingSafeEqual}from'node:crypto';
const bucket=(at=Date.now())=>Math.floor(at/300_000);export function otpFor(email:string,secret:string,at=Date.now()){const value=createHmac('sha256',secret).update(`${email.toLowerCase()}:${bucket(at)}`).digest().readUInt32BE(0)%1_000_000;return String(value).padStart(6,'0')}export function verifyOtp(email:string,code:string,secret:string,at=Date.now()){return[at,at-300_000].some((time)=>{const expected=Buffer.from(otpFor(email,secret,time));const actual=Buffer.from(code);return actual.length===expected.length&&timingSafeEqual(actual,expected)})}
