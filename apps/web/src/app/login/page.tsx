"use client"
import { signIn } from 'next-auth/react'
import { useState } from 'react'
import { appPath } from '@/lib/base-path'
import { useSearchParams } from 'next/navigation'
import { NextAuthBasePath } from '@/components/NextAuthBasePath'
function LoginForm(){const params=useSearchParams();const requested=params.get('callbackUrl');const callbackUrl=requested&&requested.startsWith(appPath('/'))&&!requested.startsWith('//')?requested:appPath('/');const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState('');async function login(event:React.FormEvent){event.preventDefault();setBusy(true);setMessage('');const result=await signIn('credentials',{email,password,callbackUrl,redirect:false});if(result?.error){setMessage('Credenciais inválidas.');setBusy(false);return}location.assign(result?.url??callbackUrl)}return <main className="login-page"><section className="login-copy"><span className="eyebrow">Rota de Ataque</span><h1>Operação comercial, sem ruído.</h1><p>Acesse leads, conversas, conteúdos e integrações em um espaço seguro e rastreável.</p></section><form className="card login-card" onSubmit={login}><h2>Entrar no Prospector</h2><label>E-mail<input type="email" autoComplete="email" value={email} onChange={event=>setEmail(event.target.value)} disabled={busy} required/></label><label>Senha<input type="password" autoComplete="current-password" value={password} onChange={event=>setPassword(event.target.value)} disabled={busy} required/></label><button disabled={busy||!email||!password}>{busy?'Entrando…':'Entrar'}</button><p role="status">{message}</p></form></main>}
export default function Login(){return <NextAuthBasePath><LoginForm/></NextAuthBasePath>}
