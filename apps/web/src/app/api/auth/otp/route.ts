import{NextResponse}from'next/server';import{ResendEmailChannel}from'@plataforma/notifications';import{otpFor}from'@/lib/otp';export async function POST(request:Request){const{email}=await request.json()as{email?:string};if(!email||!process.env.OTP_SECRET)return NextResponse.json({ok:false},{status:400});const code=otpFor(email,process.env.OTP_SECRET);if(process.env.RESEND_API_KEY&&process.env.RESEND_FROM){const channel=new ResendEmailChannel(process.env.RESEND_API_KEY,process.env.RESEND_FROM,[email]);await channel.send({kind:'Código de acesso',severity:'info',campaign:'Plataforma',message:`Seu código é ${code}. Expira em 5 minutos.`,dashboardUrl:process.env.APP_URL??'http://localhost:3000',traceId:crypto.randomUUID()})}return NextResponse.json({ok:true})}
