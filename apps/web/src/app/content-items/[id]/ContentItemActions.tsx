'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { appPath } from '@/lib/base-path'
export function ContentItemActions({id,status}:{id:string;status:string}){const router=useRouter();const[busy,setBusy]=useState<string>();const[message,setMessage]=useState('');async function act(action:'approve'|'fork'){setBusy(action);setMessage('');const response=await fetch(appPath(`/api/content-items/${id}/${action}`),{method:'POST'});const body=await response.json();if(!response.ok)setMessage(body.error??'Não foi possível concluir.');else if(action==='fork')router.push(`/content-items/${body.item.id}`);else{setMessage('Aprovado com auditoria registrada.');router.refresh()}setBusy(undefined)}return <div className="action-row"><button type="button" disabled={Boolean(busy)||status==='approved'} onClick={()=>act('approve')}>{busy==='approve'?'Aprovando…':'Aprovar canônico'}</button><button type="button" disabled={Boolean(busy)} onClick={()=>act('fork')}>{busy==='fork'?'Criando…':'Criar fork'}</button><span aria-live="polite">{message}</span></div>}
