import{createWorker,type WorkerJob,type WorkerResult,type WorkerSpec}from'@plataforma/shared/worker';export const spec={queue:'audience-overlap',requiresMetaToken:false}satisfies WorkerSpec;export const processJob=createWorker(spec);export interface OverlapPayload{scheduleId:string}export interface OverlapRepository{run(scheduleId:string,traceId:string):Promise<{updated:number}>}export function createAudienceOverlapProcessor(repository:OverlapRepository){const gate=createWorker<OverlapPayload>(spec);return async(job:WorkerJob<OverlapPayload>):Promise<WorkerResult>=>{const base=await gate(job);const result=await repository.run(job.payload.scheduleId,base.traceId);return{...base,event:{kind:'audience-overlap.completed',payload:result}}}}
