import{createDatabase,createPostgresHeartbeatStore}from'@plataforma/db';import{runWorker}from'@plataforma/queue/runtime';import{startWorkerHeartbeat}from'@plataforma/shared/worker';import{createAudienceOverlapProcessor,spec,type OverlapRepository}from'./index.js';const databaseUrl=process.env.DATABASE_URL;if(!databaseUrl)throw new Error('DATABASE_URL is required');const{pool}=createDatabase(databaseUrl);const repository:OverlapRepository={async run(scheduleId,traceId){const client=await pool.connect();try{await client.query('BEGIN');const schedule=await client.query(`SELECT * FROM crawl_schedule WHERE id=$1 AND source_type='audience_overlap' AND next_run_at<=now() FOR UPDATE`,[scheduleId]);if(!schedule.rowCount)throw Object.assign(new Error('Schedule is not due'),{reasonCode:'PREFLIGHT_FAILED'});const result=await client.query(`WITH counts AS(SELECT ls.lead_id,ls.campaign_id,COUNT(DISTINCT ls.competitor_id) FILTER(WHERE ls.source_kind='follower') followed,COUNT(DISTINCT ls.competitor_id) FILTER(WHERE ls.source_kind='comment') commented,COUNT(DISTINCT ls.competitor_id) FILTER(WHERE ls.source_kind='search') searched,COUNT(DISTINCT ls.competitor_id) FILTER(WHERE ls.source_kind='live') live FROM lead_sources ls WHERE ls.campaign_id=$1 GROUP BY ls.lead_id,ls.campaign_id),scores AS(SELECT c.*,cfg.w_overlap_followed*c.followed+cfg.w_overlap_commented*c.commented+cfg.w_overlap_search*c.searched+cfg.w_overlap_live*c.live score FROM counts c JOIN campaign_scoring_config cfg ON cfg.campaign_id=c.campaign_id) UPDATE lead_scores ls SET audience_overlap_score=s.score,computed_at=now() FROM scores s WHERE ls.lead_id=s.lead_id AND ls.campaign_id=s.campaign_id RETURNING ls.lead_id`,[schedule.rows[0].campaign_id]);await client.query(`UPDATE crawl_schedule SET last_run_at=now(),next_run_at=now()+(current_interval_seconds||' seconds')::interval,last_updated_at=now() WHERE id=$1`,[scheduleId]);await client.query(`INSERT INTO events(campaign_id,scope,level,payload) VALUES($1,'audience_overlap','info',$2)`,[schedule.rows[0].campaign_id,JSON.stringify({type:'audience_overlap.completed',updated:result.rowCount,traceId})]);await client.query('COMMIT');return{updated:result.rowCount??0}}catch(error){await client.query('ROLLBACK');throw error}finally{client.release()}}};const worker=runWorker(spec.queue,createAudienceOverlapProcessor(repository));const stop=startWorkerHeartbeat(spec.queue,createPostgresHeartbeatStore(pool),()=>({jobsDone:0,jobsFailed:0,backlog:0,p95LatencyMs:0,state:worker?.isRunning()?'running':'disabled'}));process.once('SIGTERM',()=>void stop().finally(()=>pool.end()))
