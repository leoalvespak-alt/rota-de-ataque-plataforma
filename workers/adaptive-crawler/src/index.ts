import{adaptiveInterval}from'@plataforma/shared';import{createWorker,type WorkerJob,type WorkerResult,type WorkerSpec}from'@plataforma/shared/worker';export const spec={queue:'adaptive-crawler',requiresMetaToken:false}satisfies WorkerSpec;export const processJob=createWorker(spec);export const scheduleNext=adaptiveInterval;export interface AdaptivePayload{accountId?:string}export interface AdaptiveRepository{recompute(accountId:string|undefined,traceId:string):Promise<{updated:number;budgetBlocked:number}>}export function createAdaptiveProcessor(repository:AdaptiveRepository){const gate=createWorker<AdaptivePayload>(spec);return async(job:WorkerJob<AdaptivePayload>):Promise<WorkerResult>=>{const base=await gate(job);const result=await repository.recompute(job.payload.accountId,base.traceId);return{...base,event:{kind:'adaptive-crawler.completed',payload:result}}}}
