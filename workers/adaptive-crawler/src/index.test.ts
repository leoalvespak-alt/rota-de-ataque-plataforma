import{describe,expect,it}from'vitest';import{spec,processJob}from'./index.js';describe('adaptive-crawler',()=>it('declares and enforces its worker contract',async()=>{expect(spec.queue).toBe('adaptive-crawler');expect(typeof processJob).toBe('function')}));
