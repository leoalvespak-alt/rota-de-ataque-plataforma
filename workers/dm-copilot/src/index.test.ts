import{describe,expect,it}from'vitest';import{spec,processJob}from'./index.js';describe('dm-copilot',()=>it('declares and enforces its worker contract',async()=>{expect(spec.queue).toBe('dm-copilot');await expect(processJob({id:'1',payload:{triggerKind:'cold'},preflight:{migrationsCurrent:true,embeddingDimension:384,tokenValid:true,lockAvailable:true,budgetAvailable:true,accountStatus:'HEALTHY',accountRole:'actor'}})).rejects.toThrow()}));
