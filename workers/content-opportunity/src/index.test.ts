import{describe,expect,it}from'vitest';import{spec,processJob}from'./index.js';describe('content-opportunity',()=>it('declares and enforces its worker contract',async()=>{expect(spec.queue).toBe('content-opportunity');expect(typeof processJob).toBe('function')}));
