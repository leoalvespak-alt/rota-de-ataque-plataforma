import{describe,expect,it}from'vitest';import{spec,processJob}from'./index.js';describe('search-mining',()=>it('declares and enforces its worker contract',async()=>{expect(spec.queue).toBe('search-mining');expect(typeof processJob).toBe('function')}));
