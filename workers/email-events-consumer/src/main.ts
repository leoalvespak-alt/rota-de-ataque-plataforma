import {createHash} from 'node:crypto'
import {createDatabase} from '@plataforma/db'
import {runWorker} from '@plataforma/queue/runtime'
import {createEmailEventConsumer,spec,type EmailEventRepository} from './index.js'
const databaseUrl=process.env.DATABASE_URL;if(!databaseUrl)throw new Error('DATABASE_URL is required');const{pool}=createDatabase(databaseUrl)
const repository:EmailEventRepository={async consume(event,traceId){const client=await pool.connect();try{await client.query('BEGIN');let subscriber=(await client.query<{id:string;lead_id:string|null}>(`SELECT id,lead_id FROM email_subscribers WHERE lower(email)=lower($1) OR id=(SELECT subscriber_id FROM email_events WHERE metadata->>'message_id'=$2 LIMIT 1)`,[event.email??'',event.messageId??''])).rows[0];if(!subscriber&&event.email){subscriber=(await client.query<{id:string;lead_id:string|null}>(`SELECT id,lead_id FROM email_subscribers WHERE email_hash=$1`,[createHash('sha256').update(event.email.toLowerCase()).digest()])).rows[0]}if(!subscriber){await client.query('ROLLBACK');return{inserted:false}}const inserted=(await client.query<{id:number}>(`INSERT INTO email_events(subscriber_id,kind,metadata,at,provider,external_event_id) VALUES($1,$2,$3::jsonb,$4,$5,$6) ON CONFLICT(provider,external_event_id,kind) WHERE external_event_id IS NOT NULL DO NOTHING RETURNING id`,[subscriber.id,event.kind,JSON.stringify({...event.metadata,message_id:event.messageId}),event.occurredAt,event.provider,event.externalEventId])).rows[0];if(!inserted){await client.query('COMMIT');return{inserted:false,subscriberId:subscriber.id,leadId:subscriber.lead_id??undefined}}if(['bounced','complained','unsubscribed'].includes(event.kind)){const reason=event.kind==='bounced'?'hard_bounce':event.kind==='complained'?'complaint':'unsubscribe';await client.query(`INSERT INTO email_suppressions(email_hash,reason) SELECT email_hash,$2 FROM email_subscribers WHERE id=$1 ON CONFLICT(email_hash) DO UPDATE SET reason=EXCLUDED.reason,suppressed_at=now()`,[subscriber.id,reason]);await client.query(`UPDATE email_subscribers SET unsubscribed_at=CASE WHEN $2 IN ('complained','unsubscribed') THEN now() ELSE unsubscribed_at END,hard_bounced_at=CASE WHEN $2='bounced' THEN now() ELSE hard_bounced_at END WHERE id=$1`,[subscriber.id,event.kind]);await client.query(`UPDATE email_flow_state SET status='suppressed' WHERE subscriber_id=$1 AND status='active'`,[subscriber.id])}if(event.kind==='replied'){await client.query(`UPDATE email_flow_state SET status='paused' WHERE subscriber_id=$1 AND status='active'`,[subscriber.id]);await client.query(`INSERT INTO review_inbox(item_type,item_ref_id,reason,suggested_action,context) VALUES('email_reply',$1,'Subscriber replied; human continuation required',$2::jsonb,$3::jsonb)`,[subscriber.id,JSON.stringify({action:'reply_manually'}),JSON.stringify({traceId})])}if(subscriber.lead_id){await client.query(`INSERT INTO timeline_events(lead_id,channel,event_type,external_ref,metadata,at,source) VALUES($1,'email',$2,$3::jsonb,$4::jsonb,$5,'webhook')`,[subscriber.lead_id,`email.${event.kind}`,JSON.stringify({message_id:event.messageId}),JSON.stringify(event.metadata),event.occurredAt])}await client.query('COMMIT');return{inserted:true,subscriberId:subscriber.id,leadId:subscriber.lead_id??undefined}}catch(error){await client.query('ROLLBACK');throw error}finally{client.release()}}}
runWorker(spec.queue,createEmailEventConsumer(repository))
