import{describe,expect,it}from'vitest';import{spec,processJob}from'./index.js';describe('enrichment',()=>it('declares and enforces its worker contract',async()=>{expect(spec.queue).toBe('enrichment');expect(typeof processJob).toBe('function')}));
