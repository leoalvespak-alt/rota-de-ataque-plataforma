import{describe,expect,it,vi}from'vitest';import{createWhatsAppOutboundProcessor}from'./index.js';describe('whatsapp outbound',()=>it('blocks missing opt-in',async()=>{const send=vi.fn();const process=createWhatsAppOutboundProcessor({get:async()=>({id:'m',leadId:'l',phone:'5511',kind:'text',text:'oi',status:'queued',optinActive:false,sessionActive:true,templateApproved:false,contactAllowed:true,reviewApproved:true}),sent:async()=>undefined,failed:async()=>undefined},{sendText:send}as never);await expect(process({id:'j',payload:{messageId:'m',reviewId:'r',approvedBy:'human'},preflight:{migrationsCurrent:true,embeddingDimension:384,tokenValid:true,lockAvailable:true,budgetAvailable:true,accountStatus:'HEALTHY',accountRole:'actor'}})).rejects.toThrow();expect(send).not.toHaveBeenCalled()}))
