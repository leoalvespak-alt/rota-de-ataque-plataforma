import{createDatabase,createPostgresHeartbeatStore}from'@plataforma/db';import{runWorker}from'@plataforma/queue/runtime';import{startWorkerHeartbeat}from'@plataforma/shared/worker';import{createRetentionProcessor,spec,type RetentionRepository}from'./index.js';const databaseUrl=process.env.DATABASE_URL;if(!databaseUrl)throw new Error('DATABASE_URL is required');const{pool}=createDatabase(databaseUrl);const repository:RetentionRepository={async snapshot(actionId){return(await pool.query(`SELECT e.id "actionId",e.lead_id "leadId",e.account_id "accountId",e.completed_at "followedAt",MIN(r.detected_at) "followedBackAt",COUNT(i.id)::int "interactionsAfter",NOT EXISTS(SELECT 1 FROM events ev WHERE ev.scope='retention' AND ev.payload->>'actionId'=e.id::text AND ev.payload->>'type'='retention.unfollowed') retained FROM engagement_actions e LEFT JOIN reciprocity_events r ON r.trigger_action_id=e.id LEFT JOIN lead_interactions i ON i.lead_id=e.lead_id AND i.at>e.completed_at WHERE e.id=$1 AND e.action_type='follow' AND e.status='done' GROUP BY e.id`,[actionId])).rows[0]??null},async save(snapshot,days,traceId){const column=days===7?'retained_7d':days===30?'retained_30d':days===90?'retained_90d':null;const client=await pool.connect();try{await client.query('BEGIN');await client.query(`INSERT INTO follow_retention(action_id,lead_id,account_id,followed_at,followed_back_at,followback_latency_seconds,interactions_after_count,last_checked_at) VALUES($1,$2,$3,$4,$5,CASE WHEN $5::timestamptz IS NULL THEN NULL ELSE EXTRACT(EPOCH FROM($5::timestamptz-$4::timestamptz))::int END,$6,now()) ON CONFLICT(action_id) DO UPDATE SET followed_back_at=COALESCE(EXCLUDED.followed_back_at,follow_retention.followed_back_at),followback_latency_seconds=COALESCE(EXCLUDED.followback_latency_seconds,follow_retention.followback_latency_seconds),interactions_after_count=EXCLUDED.interactions_after_count,last_checked_at=now()`,[snapshot.actionId,snapshot.leadId,snapshot.accountId,snapshot.followedAt,snapshot.followedBackAt,snapshot.interactionsAfter]);if(column)await client.query(`UPDATE follow_retention SET ${column}=$2 WHERE action_id=$1`,[snapshot.actionId,snapshot.retained]);if(!snapshot.retained)await client.query(`INSERT INTO events(account_id,scope,level,payload) VALUES($1,'retention','warn',$2)`,[snapshot.accountId,JSON.stringify({type:'retention.churned',actionId:snapshot.actionId,leadId:snapshot.leadId,checkpointDays:days,traceId})]);await client.query('COMMIT')}catch(error){await client.query('ROLLBACK');throw error}finally{client.release()}}};const worker=runWorker(spec.queue,createRetentionProcessor(repository));const stop=startWorkerHeartbeat(spec.queue,createPostgresHeartbeatStore(pool),()=>({jobsDone:0,jobsFailed:0,backlog:0,p95LatencyMs:0,state:worker?.isRunning()?'running':'disabled'}));process.once('SIGTERM',()=>void stop().finally(()=>pool.end()))
